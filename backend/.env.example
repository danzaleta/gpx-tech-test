# URL de conexión a la base de datos.
# Los valores (usuario, contraseña, puerto, nombre de la DB) deben coincidir
# con los definidos en el archivo docker-compose.yml.
DATABASE_URL="postgresql://myuser:mypassword@localhost:5432/personas_db"